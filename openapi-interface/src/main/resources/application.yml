spring:
  application:
   name: openapi-interface

server:
  port: 8081
  servlet:
    context-path: /api
# 接口文档配置
knife4j:
  enable: true
  openapi:
    title: "接口文档"
    version: 1.0
    group:
      default:
        api-rule: package
        api-rule-resources:
          - com.cheny.openapi.interface1.controller
